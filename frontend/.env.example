# Variables de entorno del Frontend (React + Vite)
# Para desarrollo local, crea un archivo .env y pon:
VITE_API_URL=http://localhost:8000

# NOTA: En producción (Railway) NO es necesario configurar esta variable,
# ya que el frontend y el backend compartirán el mismo dominio automáticamente.
